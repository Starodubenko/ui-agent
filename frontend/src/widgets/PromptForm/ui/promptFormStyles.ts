import type { Theme } from '@mui/material';

export const promptFormPaperSx = (theme: Theme) => ({
  padding: theme.spacing(4),
  maxWidth: 800,
});
export const promptFormTitleSx = (theme: Theme) => ({
  marginBottom: theme.spacing(2),
});
